package take01;

/**
 * Created by Lanessear on 29.05.2017.
 */
public class ItemWindow extends DefaultWindow {
    public ItemWindow(String title, int sizeX, int sizeY) {
        super(title, sizeX, sizeY, 0, 0, 0);
        setTitle(title + " - Items");
    }
}
